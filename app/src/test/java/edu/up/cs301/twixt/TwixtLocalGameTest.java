package edu.up.cs301.twixt;

import org.junit.Assert;
import org.junit.Test;

import static org.junit.Assert.*;

/**
 * Created by Kollin on 11/9/2017.
 */
public class TwixtLocalGameTest {
    @Test
    public void canMove() throws Exception {
        /*TwixtLocalGame local = new TwixtLocalGame();
        TwixtGameState official = new TwixtGameState();
        official.setTurn(1);
        //canMove for playerid __ should only work for one given payer id.
        int playeridx =
        Assert.AssertTrue(local.canMove(playeridx));


         */




    }

    @Test
    public void makeMove() throws Exception {
        /*
            Test if a Given Peg has been placed
        TwixtLocalGame local = new TwixtLocalGame();
        TwixtGameState official = new TwixtGameState();
        ArrayList<Peg> test = new ArrayList<Peg>();
        Peg testpeg = new Peg(1,2,0,test);
        test.add(testpeg);
        official.setBoard(test);
        PlacePegAction newpeg = new PlacePegAction(new Peg(1,2,0,test);
        makeMove(newpeg);

         */

    }

    @Test
    public void sendUpdatedStateTo() throws Exception {

    }

    @Test
    public void checkIfGameOver() throws Exception {
        /*
        //This text currently checks for failure, it will need to fully implement an Array of Pegs
        TwixtLocalGame local = new TwixtLocalGame();
        TwixtGameState official = new TwixtGameState();
        ArrayList<Peg> test = new ArrayList<Peg>();
        Peg testpeg = new Peg(1,2,0,test);
        test.add(testpeg);
        official.setBoard(test);
        Assert.AssertTrue(checkIfGameOver().equals("Player 1 won");
        */
    }

}